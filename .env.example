# Required: HMAC key for mppx stateless challenge verification
# Generate with: openssl rand -hex 32
MPPX_SECRET_KEY=

# Required: PostgreSQL connection string (Render provides this automatically)
DATABASE_URL=

PORT=3000
